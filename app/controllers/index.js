/**
 * Just maps the different files to a global
 * `controllers` module
 */
exports.svgConverter = require( __dirname + '/svgConverterController.js')
exports.graph = require( __dirname + '/graphController.js')